name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          pip install -r pipeline/requirements.txt
          pip install pytest pyyaml

      - name: Run pipeline tests
        run: pytest tests/ -v

      - name: Build index
        run: |
          python -m pipeline.build_index \
            --source docs_source \
            --fda knowledge_sources/fda_structured \
            --out /tmp/index.json \
            --semantic-out /tmp/semantic.json

      - name: Verify index integrity
        run: |
          python - <<'PY'
          import json
          idx = json.load(open('/tmp/index.json'))
          s = idx['stats']
          assert s['document_count'] >= 1
          assert s['chunk_count'] >= 1
          assert s['entity_count'] >= 1
          assert idx['bm25']['vocab']
          # Phase 2 invariants
          assert s['source_count'] >= 1, 'no connectors ran'
          assert idx['graph']['stats']['edge_count'] > 0, 'graph has no edges'
          assert 'facets' in idx, 'facets missing'
          typed = set(idx['graph']['edge_types'])
          assert typed - {'CO_OCCURS'}, 'graph has no typed edges'
          print('Index OK:', s)
          PY

      - name: Retrieval evaluation (report only on PRs)
        run: python -m eval.evaluate --index /tmp/index.json --semantic /tmp/semantic.json
